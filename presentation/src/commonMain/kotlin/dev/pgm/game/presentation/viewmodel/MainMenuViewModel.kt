package dev.pgm.game.presentation.viewmodel

import androidx.lifecycle.ViewModel

/**
 * ViewModel para la pantalla del menú principal.
 * Actualmente no gestiona estado complejo, pero se mantiene para
 * futuras expansiones (animaciones, música, etc.).
 */
class MainMenuViewModel : ViewModel() {
    // Puede expandirse para manejar animaciones del menú,
    // configuraciones, música de fondo, etc.
}
